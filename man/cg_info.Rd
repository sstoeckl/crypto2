% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cg_info.R
\name{cg_info}
\alias{cg_info}
\title{Retrieve per-coin metadata from CoinGecko (CMC-compatible columns)}
\usage{
cg_info(
  coin_list = NULL,
  limit = NULL,
  sleep = 2.5,
  wait = 60,
  max_retries = 3,
  finalWait = FALSE
)
}
\arguments{
\item{coin_list}{Tibble in \code{\link[=cg_list]{cg_list()}} / \code{\link[=cg_listings]{cg_listings()}} format (must have
a \code{slug} column). If \code{NULL}, calls \code{cg_list()}.}

\item{limit}{Optional cap on number of coins (top of the tibble).}

\item{sleep}{Seconds between calls (default \code{2.5} → 24 req/min,
~80\% of the Demo-tier 30 req/min cap, with headroom for CoinGecko's
sliding-window enforcement).}

\item{wait}{Seconds to wait before retrying after a 429 (default \code{60},
matching CoinGecko's rate-limit window). See \code{\link[=cg_make_client]{cg_make_client()}}.}

\item{max_retries}{Maximum retry attempts on 429 / network failures
(default \code{3}). See \code{\link[=cg_make_client]{cg_make_client()}}.}

\item{finalWait}{Sleep 60s after the last call (mirrors \code{crypto_info()}).}
}
\value{
Tibble with one row per coin:
\item{id}{CoinGecko internal numeric id (from \code{image$thumb}).}
\item{name, symbol, slug}{Coin identifiers.}
\item{category}{\code{"coin"} if \code{asset_platform_id} is \code{NA}, else \code{"token"}.}
\item{description}{English description (HTML stripped to text).}
\item{logo}{URL of the large coin logo.}
\item{status}{Status notice from CoinGecko (\code{public_notice} field).}
\item{notice}{Additional notices (\code{additional_notices} joined).}
\item{date_added}{Date the coin was added to CoinGecko (\code{genesis_date}
when set, else \code{NA}).}
\item{date_launched}{Same as \code{date_added} for CG (no separate launch
field exposed).}
\item{categories}{List-column of coin categories.}
\item{platforms}{List-column: per-chain contract addresses.}
\item{web_slug}{CoinGecko canonical web slug (sometimes differs from
API slug).}
\item{country_origin}{Country of project origin (often empty).}
\item{sentiment_votes_up_percentage, sentiment_votes_down_percentage}{
Community sentiment percentages.}
\item{watchlist_portfolio_users}{Number of CG users watching this coin.}
\item{url}{List-column of resource URLs (homepage, whitepaper, blockchain
explorers, forums, repos, chats, announcements, subreddit, twitter).}
}
\description{
Companion to \code{crypto_info()} but for CoinGecko. Pulls descriptive metadata
(description, categories, logos, contract addresses across chains, links)
for each coin in \code{coin_list}. Uses the documented public API endpoint
\code{api.coingecko.com/api/v3/coins/{slug}} (no key required; ~30 req/min).
}
\details{
Column names mirror \code{crypto_info()} where there is a direct equivalent,
and add a few CG-specific fields (\code{platforms}, \code{categories}).
}
\examples{
\dontrun{
info <- cg_info(cg_list(top_n = 50))
}

}
