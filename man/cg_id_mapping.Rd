% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cg_extras.R
\name{cg_id_mapping}
\alias{cg_id_mapping}
\title{Historic CoinGecko id/slug mapping (incl. delisted coins)}
\usage{
cg_id_mapping(refresh = FALSE, quiet = FALSE)
}
\arguments{
\item{refresh}{Force re-download even if a cached file exists in
\code{tempdir()}. Default \code{FALSE}.}

\item{quiet}{Suppress the one-line "historic data current until ..."
message. Default \code{FALSE}.}
}
\value{
Tibble with columns \code{id} (integer numeric CoinGecko id), \code{slug}
(character), \code{symbol} (character), \code{name} (character), \code{harvested_at}
(Date) -- one row per historic coin. Returns an empty tibble with the
correct schema if neither the network mapping nor the bundled sample
can be loaded.
}
\description{
The free CoinGecko API only exposes coins currently tracked, so coins that
get delisted after their listing day disappear from \verb{/coins/list}. A
separate, periodically updated archive of \verb{(numeric_id, slug, symbol, name, harvested_at)} rows is hosted at a stable companion location. This
function downloads and caches it so callers (e.g. \code{\link[=cg_list]{cg_list()}},
\code{\link[=cg_history]{cg_history()}}) can transparently fall back to historic identifiers
without ceremony.
}
\details{
The mapping is fetched once per session and cached in
\code{tempdir()/crypto2_cg_mapping.parquet}. If the network is unavailable, a
small bundled sample (top 100 coins, shipped in \verb{inst/extdata/}) is used
as a fallback. When \code{quiet = FALSE} (default), a single one-line message
is emitted on first successful download stating the harvest date.
}
\examples{
\dontrun{
mapping <- cg_id_mapping()
delisted <- dplyr::anti_join(mapping,
  cg_list() \%>\% dplyr::select(slug), by = "slug")
}

}
