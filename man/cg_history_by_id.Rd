% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cg_recover.R
\name{cg_history_by_id}
\alias{cg_history_by_id}
\title{Fetch CoinGecko history by numeric ID (incl. partial survivorship-bias
correction)}
\usage{
cg_history_by_id(
  ids = NULL,
  what = c("price", "market_cap", "ohlc"),
  vs_currency = "usd",
  start_date = NULL,
  end_date = NULL,
  coin_list = NULL,
  sleep = 0.6,
  wait = 60,
  max_retries = 3,
  quiet = FALSE,
  finalWait = FALSE,
  date_convention = c("end_of_day", "raw")
)
}
\arguments{
\item{ids}{Integer vector of numeric IDs to fetch. Default \code{NULL} ->
uses \code{cg_list()$id} (active universe). To extend coverage to
delisted coins, supply the union of historically-observed IDs from
your accumulated snapshots.}

\item{what}{Subset of streams to fetch. Any combination of
\code{"price"}, \code{"market_cap"}, and \code{"ohlc"}. Default all three.}

\item{vs_currency}{Quote currency, default \code{"usd"}.}

\item{start_date, end_date}{Client-side date filter applied after fetch.
\code{NULL} returns full history.}

\item{coin_list}{Optional \code{cg_list()} output used to join \code{slug} /
\code{name} / \code{symbol} onto recovered rows for coins still in the active
universe. If \code{NULL}, calls \code{cg_list()} automatically. Set
to \code{FALSE} to skip the join (rows then have only \code{id}).}

\item{sleep, wait, max_retries}{Rate-limit knobs. Defaults \code{0.6 / 60 / 3}
match \code{cg_history()}.}

\item{quiet}{If \code{FALSE}, prints a progress bar.}

\item{finalWait}{Sleep 60 s after the last call (mirrors
\code{crypto_history()}).}

\item{date_convention}{Either \code{"end_of_day"} (the default) or \code{"raw"}.
See \code{\link[=cg_history]{cg_history()}} for the explanation -- this argument applies the
same -1 day shift to midnight-UTC ticks so dates align with the
CMC / CRSP / Compustat end-of-day convention.}
}
\value{
Tibble with one row per (id, date) using crypto2-compatible
column names. Columns:
\item{id}{CoinGecko numeric id (always populated).}
\item{slug, name, symbol}{Coin identifiers -- \code{NA} for ids no longer
in the active universe (i.e. delisted on the slug side).}
\item{timestamp}{POSIXct UTC midnight of the trading day.}
\item{ref_cur_id, ref_cur_name}{Quote currency.}
\item{open, high, low, close, volume, market_cap}{Daily values.}
}
\description{
Companion to \code{\link[=cg_history]{cg_history()}} that addresses coins by their \strong{numeric
CoinGecko ID} instead of their slug. Useful in two scenarios:
}
\details{
\enumerate{
\item Coins that have been \strong{delisted} and whose slug no longer resolves.
\item Cronjob-style accumulation: when you persist \code{cg_list()} snapshots
over time, the \strong{union of all numeric IDs ever observed} is the
survivorship-bias-corrected universe. \code{cg_history_by_id()} lets you
refetch each historical ID directly without needing its current
slug to still resolve.
}

Important caveats -- please read:
\itemize{
\item \strong{The numeric-ID space is sparse, not dense.} Blind iteration over
\code{1:N} does NOT recover the full universe -- most numeric IDs in that
range have no data. The default \code{ids = NULL} therefore uses the
active universe from \code{cg_list()}, not a numeric range. To recover
delisted coins you must supply the IDs explicitly (e.g., the union
of accumulated \code{cg_list()} snapshots, or the historic mapping from
\code{\link[=cg_id_mapping]{cg_id_mapping()}}).
\item \strong{Slug recovery for delisted coins is not generally available on the
free tier.} Active coins get their slug/name joined back in from
\code{cg_list()}; rows whose numeric ID is no longer in the active universe
come back with \code{slug = NA} and \code{name = NA}. Use the \code{id} column as
the join key in downstream code. For a one-shot complete recovery of
the full historic universe see \code{vignette("coingecko-pro-backfill")}.
}
}
\examples{
\dontrun{
# Scan first 200 numeric IDs (will include both active and delisted coins)
h <- cg_history_by_id(ids = 1:200, what = c("price", "market_cap"))

# Full sweep -- survivorship-bias-free price history of the entire
# CoinGecko universe. Slow (10+ hours). Run via cronjob package.
h_all <- cg_history_by_id()
}

}
