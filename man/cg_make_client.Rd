% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cg_extras.R
\name{cg_make_client}
\alias{cg_make_client}
\title{Build a slow + insistent wrapper around \code{cg_get}}
\usage{
cg_make_client(sleep = 0.6, wait = 60, max_retries = 3, quiet = TRUE)
}
\arguments{
\item{sleep}{Seconds between successive successful calls (default 0.6 →
~100 req/min, polite for the website host; the documented
\code{api.coingecko.com} host needs \code{sleep >= 2.5} to stay safely below
the 30 req/min Demo-tier cap).}

\item{wait}{Seconds to wait before the first retry after a 429 / network
error. Defaults to 60 so the CoinGecko 60-second rate-limit window
fully resets before the retry fires. Exponential backoff applies for
subsequent retries, up to \code{pause_cap = wait * 4}.}

\item{max_retries}{Max retry attempts on failure (default 3, giving up to
~ 60 + 120 + 240 = 420 s of additional waiting in the worst case).}

\item{quiet}{If \code{FALSE}, \code{purrr::insistently} emits a message on every
retry so the caller sees the back-off in progress. Default \code{TRUE}.}
}
\description{
Combines \code{purrr::slowly} (rate limiting between successful calls) and
\code{purrr::insistently} (retry with exponential backoff on retryable
errors) to produce an HTTP client suitable for batch jobs.
}
\details{
Retry behaviour: \code{cg_get()} raises a classed condition for HTTP 429
(rate-limited) and network failures. The \code{insistently} wrapper catches
these and retries up to \code{max_retries} times, waiting \code{wait} seconds
before the first retry and up to \code{wait * 4} seconds before later
retries (with jitter). Non-retryable HTTP errors (404, 410, 5xx) still
return \code{NULL} immediately and do not consume retry budget.
}
\keyword{internal}
