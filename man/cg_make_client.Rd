% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cg_extras.R
\name{cg_make_client}
\alias{cg_make_client}
\title{Build a slow + insistent wrapper around \code{cg_get}}
\usage{
cg_make_client(sleep = 0.6, wait = 30, max_retries = 3)
}
\arguments{
\item{sleep}{Seconds between successive successful calls (default 0.6 →
roughly 100 req/min, polite for the website host; the documented host
needs \code{sleep = 2.1} to stay under the 30 req/min cap).}

\item{wait}{Seconds to wait before retry on failure (default 30).}

\item{max_retries}{Max retry attempts on failure (default 3).}
}
\description{
Combines \code{purrr::slowly} (rate limiting) and \code{purrr::insistently} (retry
with backoff) to produce an HTTP client suitable for batch jobs.
}
\keyword{internal}
