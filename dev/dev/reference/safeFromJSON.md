# Parses json data from a string without revealing information about the source in case of an error

Parses json data from a string without revealing information about the
source in case of an error

## Usage

``` r
safeFromJSON(...)
```

## Arguments

- ...:

## Value

A parsed JSON object
