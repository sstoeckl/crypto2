# Convert CoinGecko millisecond timestamps to POSIXct (UTC)

Convert CoinGecko millisecond timestamps to POSIXct (UTC)

## Usage

``` r
cg_ms_to_posix(ms)
```

## Arguments

- ms:

  Numeric vector of Unix milliseconds.

## Value

POSIXct vector, UTC.
