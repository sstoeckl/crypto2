# checks the dater format (old/new) and converts to date

checks the dater format (old/new) and converts to date

## Usage

``` r
convert_date(date_str)
```

## Arguments

- date_str:

  a date string

## Value

a correct date
